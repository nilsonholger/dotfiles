text/html;      lynx -dump -force_html %s; needsterminal; copiousoutput
application/*;  NAME="/tmp/mutt-`basename %s`" && cp %s $NAME && open $NAME && sleep 1 && rm $NAME
audio/*;        NAME="/tmp/mutt-`basename %s`" && cp %s $NAME && open $NAME && sleep 1 && rm $NAME
image/*;        NAME="/tmp/mutt-`basename %s`" && cp %s $NAME && open $NAME && sleep 1 && rm $NAME
text/*;         NAME="/tmp/mutt-`basename %s`" && cp %s $NAME && open $NAME && sleep 1 && rm $NAME
