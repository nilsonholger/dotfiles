application/pdf;    NAME="/tmp/mutt-`basename %s`" && cp %s $NAME && open $NAME && sleep 1 && rm $NAME
image/jpeg;         NAME="/tmp/mutt-`basename %s`" && cp %s $NAME && open $NAME && sleep 1 && rm $NAME
text/html;          lynx -dump -force_html %s; needsterminal; copiousoutput
